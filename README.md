# 活字排版工坊

一个纯前端本地应用，用来维护字模库、在网格版面中落字、检查字模数量是否超用、保存草稿并导出PNG预览图。

支持拼版分单：在版面设置旁选横切或竖切并填分隔行/列，把大版分成甲、乙两张小版；分隔位置有落字会标出碍事格子暂不开单，两版合计用字超库存会拒绝开单；合版预览按原坐标拼回整张，改动任一小版另一张自动标记待复核。分单与核对记录都保存在当前浏览器。

直接打开 `index.html` 即可使用，也可以在本目录启动静态服务访问。

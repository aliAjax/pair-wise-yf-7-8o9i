# 活字排版工坊

一个纯前端本地应用，用来维护字模库、在网格版面中落字、检查字模数量是否超用、保存草稿并导出PNG预览图。

支持拼版分单：在版面设置旁选横切/竖切并填分隔行（列），把整版拆成甲、乙两张小版分给两台机器开印。刀口有落字时会在版面标出碍事格子并暂缓开单；两版合计用字超出库存会被拒绝；分单后版面即合版预览（按原坐标拼回），改动任一小版另一张自动标记待复核。分单记录随草稿一起保存在当前浏览器。

直接打开 `index.html` 即可使用，也可以在本目录启动静态服务访问。
